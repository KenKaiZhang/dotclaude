# User Preferences

## Python / pip
- **Always create a new venv before running `pip install`.**
  - Use `python -m venv .venv && source .venv/bin/activate` (or the project-appropriate equivalent) before any `pip install` command.
  - Never pip install into the system or global Python environment.
